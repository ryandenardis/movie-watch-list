from django.db.models.signals import post_save, post_delete\nfrom django.dispatch import receiver\nfrom .models import Ledger\n\n@receiver(post_save, sender=Ledger)\ndef log_ledger_save(sender, instance, created, **kwargs):\n    action = 'created' if created else 'updated'\n    print(f'Ledger {action}: {instance}')\n\n@receiver(post_delete, sender=Ledger)\ndef log_ledger_delete(sender, instance, **kwargs):\n    print(f'Ledger deleted: {instance}')